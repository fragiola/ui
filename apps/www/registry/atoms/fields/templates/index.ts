import { TemplateSimple } from "./simple";

// Template namespace — access via Input.Template.Simple.
// One template per component today; namespace prepared for more.
const Template = {
    Simple: TemplateSimple,
};

export { Template };
