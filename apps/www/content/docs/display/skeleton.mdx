---
title: "Skeleton"
description: "A placeholder for loading content. `bg-palette-soft` (a role) + `animate-pulse` (Tailwind's built-in, which respects `prefers-reduced-motion` automatically). The shape is controlled by the consumer via className."
---

<InstallCommand name="skeleton" />

<ComponentPreview name="skeleton-demo" />
