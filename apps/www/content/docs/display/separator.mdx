---
title: "Separator"
description: "A visual divider. Uses Base UI's primitive for the ARIA. No family — the `menu` family has a separator member, but that one carries the menu's spacing. This separator is generic: `bg-palette-line` (a role) + orientation from the primitive."
---

<InstallCommand name="separator" />

<ComponentPreview name="separator-demo" />
