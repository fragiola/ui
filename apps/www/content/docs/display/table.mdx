---
title: "Table"
description: "Semantic HTML with palette roles. No primitive, no family. The grid is `border-palette-line`; the header and hover are `bg-palette-soft`; header text is `text-palette-contrast`; body text is `text-palette-accent/85`."
---

<InstallCommand name="table" />

<ComponentPreview name="table-demo" />
